
class Album {
	constructor(userId, id, title) {
		this.id = id;
		this.title = title;
		// Objetos con los que Album va a trabajar pero no le pertenecen
		this.userId = userId;
	}

	// Metodos del ALBUM
}